name = "kuiralabs/watch_probe"
version = "0.0.0"
compiler = "v1.1.23"
plutus = "v3"
license = "Apache-2.0"
description = "A validator with no dependencies, used to drive the watch loop"

[repository]
user = "kuiralabs"
project = "watch_probe"
platform = "github"

[config]
